import RegisterForm from './components/form';

const RegisterPage = () => {
  return (
    <div className='w-full flex-1 flex items-center justify-center py-[50px] bg-[#f5f5fa]'>
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
